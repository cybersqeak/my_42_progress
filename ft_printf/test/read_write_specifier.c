#include "header.h"


int read_write_specifier(t_info *info,va_list args)
{
    int i = 0;
//    printf("info->insident is now %s\n",info->insidents);
 //   printf("info->count is now %d\n",info->count);
    while (i < info->count)
    {
        if (check_specifier(info->insidents[i] == 'c')
        
       

}    
    
